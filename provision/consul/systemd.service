[Unit]
Description=Consul Startup process
After=network.target

[Service]
Type=simple
ExecStart=/bin/bash -c 'consul agent -config-dir SEDconsul_config_dirSED'
TimeoutStartSec=0

[Install]
WantedBy=default.target
